services:
  - type: web
    name: diabetiq-backend
    env: python
    buildCommand: cd backend && pip install -r requirements.txt && cp ../diabetes.csv . && python train_model.py
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    pythonVersion: "3.11"
    plan: free
